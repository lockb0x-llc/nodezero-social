[toolchain]
# Pinned to 1.85.0 — first stable Rust with edition 2024 support.
# Required by zeroize_derive 1.5.0 (pulled in by ark-groth16/ark-ff 0.4).
# soroban-sdk 20.x is compatible with this toolchain.
# Update this in lockstep with any soroban-sdk or ark-* version bumps.
channel = "1.85.0"
components = ["rustfmt", "clippy"]
