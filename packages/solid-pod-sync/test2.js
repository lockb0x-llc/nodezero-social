const { buildThing, createThing, setThing, mockSolidDatasetFrom } = require("@inrupt/solid-client"); const VCARD_FN = "http://www.w3.org/2006/vcard/ns#fn"; const webId = "https://example.com/profile/card#me"; let existingDataset = setThing(mockSolidDatasetFrom("https://example.com/profile/card"), buildThing(createThing({ url: webId })).setStringNoLocale(VCARD_FN, "Old Name").build()); existingDataset.internal_changeLog = { additions: [], deletions: [] }; let newDataset = setThing(existingDataset, buildThing(createThing({ url: webId })).setStringNoLocale(VCARD_FN, "New Name").build()); console.log(JSON.stringify(newDataset.internal_changeLog, null, 2));
