{
  "name": "@nodezero/jss-provisioner",
  "version": "0.0.1",
  "description": "JSS provisioning and custody attestation API",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @nodezero/solid-pod-sync build && tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "dev": "pnpm --filter @nodezero/solid-pod-sync build && tsx src/index.ts",
    "test": "pnpm --filter @nodezero/solid-pod-sync build && tsx --test src/**/*.test.ts",
    "lint": "eslint src --ext .ts",
    "type-check": "pnpm --filter @nodezero/solid-pod-sync build && tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nodezero/solid-pod-sync": "workspace:*",
    "@nodezero/zk-crypto": "workspace:*",
    "@stellar/stellar-sdk": "^16.0.0",
    "snarkjs": "0.7.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.4.5"
  }
}
