{
  "name": "@nodezero/css-stellar-auth",
  "version": "0.1.0",
  "description": "RETIRED (internal-auth cutover): legacy CSS v7 login-page plugin. The provisioner verify endpoint it depends on no longer exists; the CSS interactive login surface is disabled. Kept only until the solid-server image build drops it.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "lsd:module": "https://linkedsoftwaredependencies.org/bundles/npm/@nodezero/css-stellar-auth/0.1.0/",
  "lsd:components": "components/context.jsonld",
  "files": ["dist", "components", "config"],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "echo '[css-stellar-auth] RETIRED: legacy CSS login plugin excluded from the workspace gate (internal-auth cutover).'",
    "type-check": "echo '[css-stellar-auth] RETIRED: legacy CSS login plugin excluded from the workspace gate (internal-auth cutover).'",
    "clean": "npx rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
