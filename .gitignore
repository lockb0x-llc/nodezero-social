node_modules/
dist/
.expo/
*.log
*.tgz
.env
.env.local
.env.*.local
coverage/
.turbo/
target/
build/
*.wasm

# Parallel-agent worktrees (created by scripts/agents/dispatch-parallel.ps1)
.agent-worktrees/

# Dev-only credentials and scratch files — never commit these
docs/dev-only/

# Playwright MCP session artifacts (tool output, not project files)
.playwright-mcp/
playwright-report/
playwright-results/

# Generated ARM JSON output (local build artifact)
infrastructure/azure/main.json
packages/jss-provisioner/deploy.zip
packages/mobile-app/dist.zip

# Provisioner Azure log download directories (local debug artifacts)
provisioner-logs/
provisioner-logs-live/
provisioner-logs/latest.zip

# Soroban test snapshots emitted by cargo test --target x86_64-unknown-linux-gnu
packages/contracts/test_snapshots/
docs/screenshots/onboarding-validation-2026-06-29
